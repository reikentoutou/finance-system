node_modules/
dist/
*.db
*.db-journal
.env
# 仅忽略仓库根目录 uploads；勿写 uploads/，否则会误伤 apps/api/dist/uploads，桌面包 pnpm deploy 会丢文件
/uploads/
.DS_Store
apps/api/uploads/

# 本机 `npx skills add` 等生成的技能包（体积大；需要团队统一时可改为提交）
.agents/
skills/
skills-lock.json
